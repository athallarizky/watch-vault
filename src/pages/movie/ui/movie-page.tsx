// import { useParams } from "@tanstack/react-router";

export const MoviePage = () => {
	return <div>test</div>;
	// const { movieId } = useParams({ from: "/movies_/$movieId" });
	// const details = useMovieDetails(Number(movieId));
};
